package MapDemo;

/**
 * HashTable 基本被淘汰
 * 与HashMap类似，不同的是，它不允许记录的键或值为空，
 * 支持线程同步，即任意时刻只能有一个线程写HashTable，
 * HashTable为了实现多线程安全，在几乎所有的方法上都加了synchronized锁，而加锁的结果就是Hashtable操作的效率十分低下。
 */

public class HashTableDemo {
    public static void main(String[] args) {

    }
}
